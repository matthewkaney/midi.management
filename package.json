{
  "name": "midi-management",
  "version": "0.0.0",
  "scripts": {
    "clean": "rm -rf .parcel-cache public",
    "dev": "parcel serve app/index.html --dist-dir public",
    "build": "parcel build app/index.html --dist-dir public --no-source-maps",
    "deploy": "npm run clean && npm run build && firebase deploy"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@musedlab/midi": "0.0.5",
    "@reach/router": "^1.3.4",
    "firebase": "^8.2.4",
    "immer": "^9.0.6",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "tone": "^13.8.25"
  },
  "devDependencies": {
    "@parcel/transformer-image": "^2.0.0-rc.0",
    "@types/reach__router": "^1.3.7",
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "firebase-tools": "^9.2.2",
    "parcel": "^2.0.0-rc.0",
    "typescript": "^4.1.3"
  }
}
